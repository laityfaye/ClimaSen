# Dependances du backend Jarvis uniquement.
# Le dashboard garde son propre requirements.txt a la racine.
anthropic>=1.7.0
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2

# Tests
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27          # requis par fastapi.testclient
